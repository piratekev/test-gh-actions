name: Manually triggered workflow
on:
  push:
    branches:
      - 'release-*'


jobs:
  log-github-info:
    runs-on: ubuntu-latest
    steps:
      - run: echo ${{ github.event.before }} ${{ github.event.after }} ${{ github.event.created }} ${{ github.event.commits }}
